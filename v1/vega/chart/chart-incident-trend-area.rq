PREFIX ai: <https://iqns.org/v1/ai#>

SELECT ?date ?incidentType (COUNT(?incident) AS ?count) WHERE {
  ?incident a ai:Incident ;
            ai:reportedDate ?date ;
            ai:incidentType ?incidentType .
}
GROUP BY ?date ?incidentType
ORDER BY ?date
